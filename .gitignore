.env
*.env
!.env.example

# Runtime caches. Never commit either of these.
#
# cache/ holds controller snapshots: a MAC, hostname and IP inventory of every
# device on the network. Regenerable with `unifi-map fetch`.
#
# It also holds Ubiquiti's device catalog and downloaded product artwork.
# That artwork is Ubiquiti's intellectual property and is deliberately NOT
# vendored into this repository; it is fetched at runtime and cached locally.
# Snapshots and renders. The glob matters: a snapshot is a full inventory of a
# network (every MAC, hostname, address and SSID), and the obvious thing to do
# before a risky fetch is `cp -r cache cache.bak`, which `cache/` alone does not
# cover. That exact backup sat untracked in this repository once, one `git add
# -A` away from being published.
cache*/
out*/

# `make demo` renders straight into examples/demo/, alongside the committed
# input snapshot, rather than a separate out/ tree: the two used to live in
# different top-level directories and that read as duplication even though
# only the input was ever committed. Rendered output embeds Ubiquiti's
# artwork as base64, so it must never reach git either way; matched by
# extension rather than a subdirectory, since there is no subdirectory here
# to gitignore. The input files (*.json, overrides.toml) do not use any of
# these extensions, so this cannot hide real fixture data by accident.
examples/demo/*.svg
examples/demo/*.pdf
examples/demo/*.drawio
examples/demo/*.html

# Build artifacts from `make build`. Regenerable, and a built wheel committed by
# accident would be a second copy of the source going stale in the tree.
dist/
build/

.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.coverage
coverage.xml

# Editor scratch files
*.sw[a-p]
*~
.*.swp
