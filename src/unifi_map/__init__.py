"""Export a UniFi network topology as zoomable vector diagrams."""

# The single source of truth for the version. pyproject.toml reads this
# attribute, so there is nothing to keep in step by hand.
#
# Semantic versioning, currently pre-1.0: the minor number moves for new
# behaviour, the patch number for fixes, and anything may still change until
# 1.0. See CHANGELOG.md.
__version__ = "0.10.1"
